Nota de limpieza y arranque

He eliminado `node_modules/` y `package-lock.json` que se encontraban en la raíz del repo por error. Todas las dependencias del frontend están ahora dentro de `front_sos911/`.

Comandos para trabajar con el frontend:

```bash
cd front_sos911
npm install
npm run dev
```

Alternativa (desde la raíz):

```bash
npm --prefix front_sos911 run dev
```

Si prefieres que añada esta nota directamente en `README.md`, puedo intentarlo de nuevo y dejarlo exactamente como quieras.